//1. declares an empty class called students
//2. adds an emty constructor method  to the student class
//3. adds a parameter called gpa to the constructor method, and then create a gpa property and set it equal to the parameter

class Student {
  constructor(gpa) {
    this.gpa = gpa;
  }
}
