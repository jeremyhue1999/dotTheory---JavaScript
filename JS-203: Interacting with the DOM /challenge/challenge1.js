//Selects the button with the ID makeItRed and assign it to the button variable.
//adds a click event listener to button. Pass an empty function as the second parameter.
//applies a background color of "red" to the warning div.
var warning = document.getElementById("warning");
var button = document.getElementById("makeItRed");

button.addEventListener("click", () => {});
