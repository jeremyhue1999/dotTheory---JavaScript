//Creates a while loop that logs the current value of count to the console 26 times.
//Uses the count variable to track the number of times the loop runs.

let count = 0;

while (count < 26) {
  console.log(count);
  count++;
}
