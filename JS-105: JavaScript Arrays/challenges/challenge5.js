// creates a loop to iterate over the values inside the temperatures array.
const temperatures = [100, 90, 99, 80, 70, 65, 30, 10];

for (let i = 0; i < temperatures.length; i++) {
  console.log(temperatures[i]);
}
